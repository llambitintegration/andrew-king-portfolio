# Diana Keffer Portfolio - Environment Configuration Template
# Copy this file to .env.local and configure for your environment

# Admin Authentication (Optional - falls back to demo credentials if not set)
NEXT_PUBLIC_ADMIN_EMAIL=your-admin-email@example.com
NEXT_PUBLIC_ADMIN_PASSWORD=your-secure-password

# Development Performance Settings for Windows
NEXT_TELEMETRY_DISABLED=1

# Webpack Configuration (automatically handled by next.config.mjs)
# These are for reference only - actual settings are in next.config.mjs
# WEBPACK_CACHE_TYPE=filesystem
# WEBPACK_POLLING=false

# Database URLs (if adding backend functionality later)
# DATABASE_URL=your-database-url
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-nextauth-secret

# External API Keys (if integrating external services)
# GOOGLE_ANALYTICS_ID=your-ga-id
# SENTRY_DSN=your-sentry-dsn

# Vercel/Deployment Settings
# VERCEL_URL will be automatically set in production
